# Chocodist - wiht many code comments

The app has covers:
 - handling producers - CRUD actions
 - handling products - CRUD actions

It uses a SQLite DB.

The interaction with the DB is via ORM interface of the SQLAlchemy.

App is organized as a MVC (Model-View-Controller) App:

<img width="592" height="366" alt="image" src="https://github.com/user-attachments/assets/9d1c72f2-688a-4ac5-941f-e3a7e035af3a" />

The code contain many comments - goal is to provide implementation details that seemed relevant at implement time

Creating a release with all these comments. They mignt be helpfull.
Going forward I'll delete them - they are too many ...



